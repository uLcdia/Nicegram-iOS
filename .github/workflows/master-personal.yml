name: Master Personal Build

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: macos-latest

    env:
      BAZEL_USER_ROOT: /private/var/tmp/_bazel_telegram
      BUILD_WORKING_DIR: /Users/telegram/build-working-dir
      SOURCE_PATH: /Users/telegram/telegram-ios

    steps:
      # Step 1: Checkout the main repository (without submodules)
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: '0'

      # Step 2: Initialize Submodules (SKIPPING tgcalls)
      - name: Initialize Submodules (Skip tgcalls)
        run: |
          set -x
          git submodule init
          git -c submodule."submodules/TgVoIPWebrtc/tgcalls".update=none submodule update --recursive
          
      # Step 3: Set the correct Xcode version
      - name: Set active Xcode path
        run: |
          XCODE_VERSION=$(cat versions.json | python3 -c 'import json,sys;obj=json.load(sys.stdin);print(obj["xcode"]);')
          sudo xcode-select -s /Applications/Xcode_$XCODE_VERSION.app/Contents/Developer

      # Step 4: Create the same directory structure the build expects
      - name: Create canonical source directory
        run: |
          set -x
          sudo mkdir /Users/telegram
          sudo chown -R $(whoami) /Users/telegram
          cp -R $GITHUB_WORKSPACE /Users/telegram/
          mv /Users/telegram/$(basename $GITHUB_WORKSPACE) /Users/telegram/telegram-ios

      # Step 5: Create the build configuration file from secrets
      - name: Create Build Configuration
        run: |
          echo "Creating my_config.json"
          # We create the JSON file that --configurationPath requires.
          # We must include bundleId and teamId so the script's
          # validation passes, even if they aren't used for signing.
          echo '{ \
            "apiKey": "${{ secrets.API_ID }}", \
            "apiHash": "${{ secrets.API_HASH }}", \
            "bundleId": "app.nicegram", \
            "teamId": "DUMMYTEAMID" \
          }' > $SOURCE_PATH/my_config.json
          
          echo "Configuration file created:"
          cat $SOURCE_PATH/my_config.json

      # Step 6: Build Unsigned App (with all required arguments)
      - name: Build Unsigned App with Make.py
        run: |
          set -x
          cd $SOURCE_PATH
          
          # This command provides all the arguments the script requires:
          # --buildNumber: We use the GitHub run number.
          # --configurationPath: We use the JSON we just created.
          # --xcodeManagedCodesigning: Satisfies the codesigning requirement.
          # --bazelArguments: This is the magic. We pass the "disable" flag
          #   directly to Bazel, bypassing the Make.py script's logic.
          
          python3 build-system/Make/Make.py build \
            --buildNumber ${{ github.run_number }} \
            --configuration=release_arm64 \
            --configurationPath $SOURCE_PATH/my_config.json \
            --xcodeManagedCodesigning \
            --bazelArguments="--//Telegram:disableProvisioningProfiles"
            
          # Find the .app file. The path is complex.
          APP_PATH=$(find "bazel-out" -path "*Release-arm64/bin/Telegram/Telegram.app" | head -n 1)
          if [ -z "$APP_PATH" ]; then
            echo "::error::Could not find .app file in bazel-out"
            exit 1
          fi
          echo "APP_PATH=$APP_PATH" >> $GITHUB_ENV
          
          # Set the final IPA path
          mkdir -p $BUILD_WORKING_DIR/ipa
          echo "IPA_PATH=$BUILD_WORKING_DIR/ipa/Nicegram.ipa" >> $GITHUB_ENV

      # Step 7: Manually package the unsigned .app into a .ipa
      - name: Package Unsigned IPA
        run: |
          set -x
          mkdir -p "Payload"
          cp -R "${{ env.APP_PATH }}" "Payload/Nicegram.app"
          zip -r "${{ env.IPA_PATH }}" "Payload"

      # Step 8: Upload the .ipa as a build artifact
      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Nicegram-unsigned
          path: ${{ env.IPA_PATH }}
          retention-days: 7
